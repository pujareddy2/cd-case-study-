#ifndef SYMBOLTABLE_H
#define SYMBOLTABLE_H

/* Function to generate a new temporary variable (t1, t2, ...) */
char* new_temp();

/* Function to generate a new label (L1, L2, ...) */
char* new_label();

#endif
